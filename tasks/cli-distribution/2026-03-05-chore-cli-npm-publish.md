---
status: open
type: chore
created: 2026-03-05
---

# Publish CLI to npm and update Claude skill

Add the CLI to npm so users can install it via `npm install` / `npx`. Update the Claude flashcards skill to use the npm-published CLI instead of the current approach.
